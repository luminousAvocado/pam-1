@model Location
@{
  ViewData["Title"] = "View Location";
  var isAllowed = (bool)ViewData["Admin"];
}

<nav aria-label="breadcrumb">
  <ol class="breadcrumb">
    <li class="breadcrumb-item"><a asp-action="Locations">Locations</a></li>
    <li class="breadcrumb-item active" aria-current="page">@Model.Name</li>
  </ol>
</nav>
@if (isAllowed)
{
  <p class="text-right"><a asp-action="EditLocation" asp-route-id="@Model.LocationId" class="btn btn-primary">Edit Location</a></p>
}

<table class="table table-bordered">
  <tr><th>Id</th><td>@Model.LocationId</td></tr>
  <tr><th>Name</th><td>@Model.Name</td></tr>
  <tr><th>Address</th><td>@Model.Address</td></tr>
  <tr><th>City</th><td>@Model.City</td></tr>
  <tr><th>State</th><td>@Model.State</td></tr>
  <tr><th>Zip</th><td>@Model.Zip</td></tr>
</table>
